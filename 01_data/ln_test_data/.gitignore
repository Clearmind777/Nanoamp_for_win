# 这些文件是 test_data/ 的逐字节副本，不再提交，克隆后重建即可：
#
#     Rscript 03_dependence/r-environment/materialize_test_data.R
#
# manifest.tsv 记录了每个副本对应的原始文件，重建脚本按 md5 校验，
# 缺一个都会报错退出，所以不会有"少文件但没人发现"的情况。
#
# 保留在 Git 里的是 .fa：它们不是逐字节副本，而是把公司的 .seq/.ab1
# 解析后重新写出的 FASTA，重建脚本只能复制、不能重新解析。
*.fastq
*.xlsx
*.ab1
