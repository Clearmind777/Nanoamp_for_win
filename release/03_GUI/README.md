# 图形界面版（GUI）

给**不写代码的人** —— 也是绝大多数情况推荐的版本。

## 里面有什么

```text
03_GUI/
|-- README.md
`-- nanoamp.exe          图形界面程序（约 10 MB，双击即可运行）
```

## 安装

**推荐：双击 `../install.exe`。** 它会把界面装到
`%LOCALAPPDATA%\nanoamp\app\nanoamp.exe`，并在桌面创建
**「nanoamp 分析工具」** 快捷方式。

装完双击桌面图标就能用。

## 不安装也能试（前提是已经装过）

如果已经跑过一次 `install.exe`（R 和依赖都就位了），
直接双击本目录下的 `nanoamp.exe` 也能打开窗口。

> `nanoamp.exe` 只包含界面本身（约 10 MB），**不含 R 运行时**。
> R 和 109 个依赖包由 `install.exe` 安装。这样做的代价是要先装 R，
> 好处是安装包小、启动快。

## 界面

```text
┌─────────────────────────────────────────────────────────┐
│  nanoamp - 纳米孔 PCR 产物分析                            │
├─────────────────────────────────────────────────────────┤
│  输入                                                    │
│   测序文件 (FASTQ)  [__________________] [浏览…]         │
│   目的序列 (FASTA)  [__________________] [浏览…]         │
│   输出目录          [__________________] [浏览…]         │
│   模式 [A - 参考引导 ▾]   显示前 n 条 [20]                │
├─────────────────────────────────────────────────────────┤
│  [单倍型结果] [QC 指标] [输出文件] [运行日志]              │
│  排名 │ 编号 │ reads 数 │ 占比 │ 是否一致 │ 变异 │ ...    │
│  选中某一行会显示这条序列的完整碱基                        │
├─────────────────────────────────────────────────────────┤
│  [开始分析] [环境自检]              [打开输出目录]        │
│  状态：就绪。选择 FASTQ 和参考序列后点击"开始分析"。        │
└─────────────────────────────────────────────────────────┘
```

### 操作步骤

1. **测序文件**：选公司给的 `.fastq`。
   若同目录下有 `reference.self.fa`，选完 fastq 会自动填上参考。
2. **目的序列**：选你的 `.fa` / `.fasta`。
3. **输出目录**：默认 `我的文档\nanoamp 结果`，一般不用改。
4. **模式**：保持 `A - 参考引导（推荐）`。**显示前 n 条** 保持 20。
5. 点 **「开始分析」**，等状态栏显示"分析完成"。

### 四个标签页

| 标签 | 内容 |
|---|---|
| **单倍型结果** | 核心结果表。选中某行，下方显示该序列完整碱基 |
| **QC 指标** | reads 数、比对率、平均一致度等质量指标 |
| **输出文件** | 本次生成的文件，双击用系统默认程序打开 |
| **运行日志** | R 的实时输出，出错时看这里 |

### 三个按钮

| 按钮 | 作用 |
|---|---|
| **开始分析** | 启动分析（后台线程执行，窗口不会卡死） |
| **环境自检** | 跑 `nanoamp doctor`，把 R / 依赖 / minimap2 的状态写进日志 |
| **打开输出目录** | 在资源管理器里打开结果文件夹 |

## 怎么看结果

「单倍型结果」表的列含义：

| 列 | 含义 |
|---|---|
| 排名 | 按 reads 数从多到少 |
| 编号 | H1、H2… 每种不同序列的编号 |
| **reads 数** | 支持这条序列的 reads 条数 |
| **占比** | 占全部 reads 的百分比 ← 最关心的数值 |
| **是否与目的序列一致** | `是` = 和你要的序列完全一样 |
| SNV / 插入 / 缺失 | 相对目的序列有几处变异 |
| 变异 | 具体描述，如 `218delG` = 第 218 位少一个 G |
| 长度 | 序列碱基数 |

**读法示例：**

```text
排名  编号  reads 数  占比     是否一致   变异
 1    H1     142    33.3%     否       218delG
 2    H2     134    31.5%     是       .
 3    H3     114    26.8%     否       218delG;135C>T
```

→ 这个样本里 **31.5% 是目的序列**；最多的一种（33.3%）少了一个 G；
第三多的（26.8%）同时少一个 G 且有一个 C→T 突变。

> 选中某一行若显示"未导出序列"，是因为 `haplotypes.fasta` 只保存前 n 条
> （默认 20 条）。把「显示前 n 条」调大后重新运行即可。

## 常见问题

**双击没反应 / 一闪而过**
说明 R 或依赖没装好。先双击 `../install.exe` 跑一次安装。

**提示找不到 R**
`%LOCALAPPDATA%\nanoamp\config.ini` 里的 `rscript=` 指向的路径失效了。
重新运行 `install.exe`。

**分析失败**
看「运行日志」标签页最后几行。最常见原因是
①相应文件被占用，②输出目录没有写权限，③输入 FASTQ 格式不对。

**中文路径出错**
输入/输出路径尽量用英文且不含空格，例如 `D:\nanoamp_data\`。

**结果里几乎没有"是否一致 = 是"**
先看「QC 指标」的 `n_reads_total`：低于 50 条时比例不可靠。
其次确认目的序列选对了（本样本的预期序列，不是别的样本的）。

## 技术说明（给开发者）

- 界面用 **Python + Tkinter**：Tkinter 随 CPython 提供，无运行时依赖，
  冻结后只有约 10 MB（对比 PySide6 会到 100 MB 以上）。
- **不重写任何算法**：界面通过子进程调用 R 的 `nanoamp_cli()`，
  与命令行完全一致，因此两者结果不可能不一致。
- 打包：`python 06_GUI/build_exe.py`（PyInstaller，one-file、windowed）。
- 安装后由 `%LOCALAPPDATA%\nanoamp\config.ini` 的 `home=` 告知数据目录位置。
- 源码与打包细节见 `06_GUI/README.md`。

## 另一个图形界面（R Shiny）

仓库里还有一套基于 R Shiny 的界面（`02_code/gui/`），功能更全
（参数更多、DT 交互表格、下载按钮），但需要浏览器和 shiny/DT 依赖，
不能双击即用。两者共用同一套分析核心。日常使用推荐本目录的 Tkinter 版。
